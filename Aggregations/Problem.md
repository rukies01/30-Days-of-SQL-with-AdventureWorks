Problem

The goal of Day 5 was to practice business-focused aggregations using SQL. The tasks were:

Calculate total sales per year.

Determine the average order value per customer.

Count the number of orders per region.

The objective was not just writing queries, but thinking about data grain, business logic, and how these metrics would appear in a real dashboard
