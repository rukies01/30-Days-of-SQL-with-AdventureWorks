The focus of Day 19 was understanding when to use window functions instead of aggregation and how window functions preserve row-level detail while still allowing analytical calculations across groups of data.

Tasks
Show total sales per customer without losing row detail
Use window functions to display customer-level totals while still retaining individual transaction records.
Compare order value to customer’s average order value
Calculate the value of each order and compare it against the customer’s average order value using layered aggregation and window functions.
Understand when window functions are better than aggregation
Explore how GROUP BY reduces data into summarized rows, while window functions allow calculations such as totals, averages, rankings, and comparisons without removing row-level detail.
